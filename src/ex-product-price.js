let quantidade = 35;

let price = 5.5;
if (quantidade >= 25) {
  price = 4.85;
  console.log(price * quantidade);
} else {
  console.log(price * quantidade);
}

/**
 * let preco = quantidade < 25 ? 5.5 : 4.85;
 * console.log(preco * quantidade);
 */
